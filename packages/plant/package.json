{
  "name": "plant-radish",
  "version": "0.1.3",
  "type": "module",
  "module": "build/index.js",
  "license": "MIT",
  "prettier": "@radish/prettier",
  "devDependencies": {
    "@radish/prettier": "*",
    "@radish/tsconfig": "*",
    "typescript": "^4.6.2"
  },
  "scripts": {
    "clean": "rm -rf build tsconfig.tsbuildinfo",
    "build": "yarn clean && yarn tsc",
    "prepare": "yarn build"
  },
  "bin": {
    "plant-radish": "./bin/cli.js"
  }
}
